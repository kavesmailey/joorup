Backend: Cloudflare Workers (رایگان ۱۰۰k req/day)
Dashboard: همان Worker + Static Assets
DB اولویت:

Telegram Cloud
Cloudflare D1
Cloudflare KV

AI: Grok API (اصلی)
محدودیت فاز ۱: فقط Cloudflare Workers. هیچ سرویس دیگری اضافه نشود.
کد باید Node.js ساده باشد تا بعداً به سرور ایرانی قابل انتقال باشد.
